import type { ExistingItemState } from '../interfaces/existing-item-state.interface';

export const DEFAULT_EXISTING_ITEM_STATE: ExistingItemState = {
  name: 'Item',
  description: null,
  category: 'uncategorized',
  priority: null,
  metadata: null,
};
