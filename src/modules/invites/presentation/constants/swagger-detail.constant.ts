export const INVITES_SWAGGER_DETAIL = {
  tags: ['Invites'] as string[],
  security: [{ bearerAuth: [] as string[] }],
};

export const INVITES_PUBLIC_SWAGGER_DETAIL = {
  tags: ['Invites'] as string[],
};
