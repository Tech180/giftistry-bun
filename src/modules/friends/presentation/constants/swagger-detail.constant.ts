export const FRIENDS_SWAGGER_DETAIL = {
  tags: ['Friends'] as string[],
  security: [{ bearerAuth: [] as string[] }],
};

export const USERS_SWAGGER_DETAIL = {
  tags: ['Users'] as string[],
  security: [{ bearerAuth: [] as string[] }],
};
