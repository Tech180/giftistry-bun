export const NOTIFICATION_SELECT = `
  id as "Id", user_id as "UserId", type as "Type", title as "Title", body as "Message",
  metadata as "Metadata", read_at as "ReadAt", created_at as "CreatedAt"
`;
