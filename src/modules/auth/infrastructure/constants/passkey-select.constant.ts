export const PASSKEY_SELECT = `
  id as "Id", user_id as "UserId", credential_id as "CredentialId", public_key as "PublicKey",
  counter as "Counter", backed_up as "BackedUp", transports as "Transports"
`;
